import { ReactElement } from 'react';

export default function Header(): ReactElement {
	return (
		<header className="h-32 flex justify-center items-center">
			<h1 className="text-white text-6xl font-medium">Sistema Solar</h1>
		</header>
	);
} 